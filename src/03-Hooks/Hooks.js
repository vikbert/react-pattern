import React from 'react';
import Nav from '../Nav';
import LoginForm from './useRef/LoginWithAutoFocus';

const Hooks = () => (
    <>
        <Nav/>
        <h1>React Hooks</h1>
        <h2>useRef()</h2>
        <LoginForm/>

    </>
);

export default Hooks;
